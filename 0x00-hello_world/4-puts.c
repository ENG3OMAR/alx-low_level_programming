#include <stdio.h>

/**
 * main - Entry point
 * this is my c prog touse puts function
 * and it returns zero at the end
 * Return: Always 0 (Sucess)
 */

int main(void)
	{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
	}
